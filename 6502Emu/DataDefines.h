#pragma once

// Define data sizes for our cpu because we can and to keep things simple
using Byte = unsigned char;		// A single byte
using Word = unsigned short;	// A word (two bytes)

using u32 = unsigned int;